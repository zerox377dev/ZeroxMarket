# ZeroxMarket

Private distribution and auto-update hosting repository.

## Structure

Each app has its own directory containing a `releases.json` manifest:

```
/<app-name>/releases.json
```

## AdGuard

DNS allowlists for Firebase and other services:

```
/adguard/firebase_allowlist.txt
```
